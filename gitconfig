[github]
	user = mina-alber

[user]
	name = Mina Alber
	email = me@mina-soft.com

[alias]
	ck = checkout
	cm = commit -m
	pom = push origin master
	plom = pull origin master
	pod = push origin development
	plod = pll origin development
	logg = log --oneline --graph --color --decorate
	lg = log --oneline
	stu = status -s

[push]
	default = current

[pull]
	default = current
